#include <stdio.h>
void main()
{
    char a[1000];
    scanf("%s",&a);
    for(char i in a)
    {
        c=0
        if(i=='a'|i=='e'|i=='o'|i=='i'|i=='u')
        {
            c+=1;
        }
    }
    {printf("%d",c);}
}