#include<stdio.h>
int main()
{
    int n,a,k;
    sanf("%d",&n);
    int s[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a);
        s[i]=a;
    }
    for(int j=a-b;j<a;j++)
    {
        printf("%d",s[j]);
    }
    for(int k=0;k<a-b;k++)
    {
        printf("%d",s[k]);
    }
}